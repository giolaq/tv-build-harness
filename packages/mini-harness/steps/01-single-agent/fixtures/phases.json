{
  "phases": [
    {
      "name": "screen",
      "prompt": "Read the existing Pocket Cinema React Native app. Replace the placeholder with a horizontal catalog screen and a reusable ShowCard component.",
      "verify": { "type": "file_exists", "path": "out/src/components/ShowCard.tsx" }
    },
    {
      "name": "content",
      "prompt": "Add three synthetic shows to the catalog. One title must be Kitchen Stories.",
      "verify": { "type": "grep", "path": "out/src/catalog.ts", "pattern": "Kitchen Stories" }
    },
    {
      "name": "focus",
      "prompt": "Add visible TV focus feedback to ShowCard without changing its public props. The component must handle onFocus.",
      "verify": { "type": "grep", "path": "out/src/components/ShowCard.tsx", "pattern": "onFocus" }
    }
  ]
}
