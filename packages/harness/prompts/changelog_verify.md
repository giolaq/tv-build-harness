# Changelog Site: Verify

Run the static site's local verification command.

Expected command:

```sh
npm run build
```

If it fails, fix the generated site or package scripts. Do not replace the changelog site with a TV app.
