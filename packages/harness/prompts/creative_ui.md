You are an award-winning TV UI designer. Make this app visually UNFORGETTABLE and completely distinct from any other app generated by this system. The app currently looks like a template — give it a strong visual identity.

The app has correct layouts and navigation. Do NOT change structure or navigation. Dramatically enhance the visual design.

PRIORITY ORDER (do these first — they have the most visual impact):
1. Focus state glow/effects (the element users see CONSTANTLY)
2. Typography (display font for titles — instant personality)
3. Surface hierarchy (cards ≠ background — depth)
4. Atmospheric component (gradient overlay — premium feel)
5. Section polish (spacing, accent lines — refinement)
6. Per-screen variation (if time permits)

## STEP 1: Discover the visual DNA

Use the loaded skill to find:
- Theme and color files
- Screen files (HomeScreen and at least one other)
- Card/tile component

Read them before making any changes.

## STEP 2: Define a unique visual personality

This app is "{{appName}}" — {{prompt}}

Brand: primary={{primaryColor}}, accent={{accentColor}}, background={{backgroundColor}}

Every app must have a **visual signature** — one distinctive element that makes it instantly recognizable. Choose based on the content:

| Content Type | Visual Signature Ideas |
|---|---|
| Cooking/Food | Warm golden overlays, organic rounded shapes, appetizing warm gradients |
| Sports/Live | Diagonal cuts, high-energy gradient stripes, stadium spotlight effects |
| Music/Entertainment | Neon glow bleeds, glass-morphism cards, equalizer-inspired spacing |
| Fitness/Health | Geometric precision, progress-bar dividers, sharp athletic angles |
| News/Documentary | Editorial grid, serif display type, ink-black depth |
| Kids/Animation | Bouncy oversized elements, candy gradients, thick rounded outlines |

Commit to a direction. Be bold — safe is boring.

{{creativeSeed}}

## STEP 3: Cinematic scrim + atmospheric layer

### 3a: Hero scrim (MANDATORY if the app has a hero/featured section)
Find the hero/featured component. Add a cinematic scrim — a gradient overlay between the background image and any text. The loaded skill has the exact pattern (Cinematic Scrim System section). Without a scrim, text on images fails the 4.5:1 contrast requirement.

### 3b: Atmospheric component
Create a new atmospheric component that adds depth to screens. Use the loaded skill to find the right location.

Options (pick one or combine):
- **WarmGlowBackground**: Radial gradient overlay at low opacity (3-8%) for atmospheric tint
- **AccentLineDecoration**: Thin accent-colored lines or geometric shapes at key positions
- **SurfaceCard**: Card wrapper with surface-hierarchy colors and subtle depth

Use this component in at least HomeScreen and one other screen.

### 3c: Color desaturation check
TV panels render colors more saturated than monitors. Check that the accent color is not over-saturated — if it's pure/vivid (e.g., #FF0000, #00FF00, #FFAA00), reduce saturation by 15-20%. The loaded skill's TV Panel Color Physics section explains why.

## STEP 4: Typography enhancement

Install complementary fonts if needed using the loaded skill's font installation pattern. Two complementary fonts:
1. A DISPLAY font (bold, distinctive) for hero titles and section headings
2. A BODY font (clean, readable) for descriptions and metadata

Good pairings: Oswald/Nunito Sans, Bebas Neue/DM Sans, Playfair Display/Source Sans Pro, Space Grotesk/Inter, Archivo Black/Lato.

Update the theme typography to use the new fonts.

## STEP 5: Focus state as signature element

The focus state is the most-seen UI element on TV. Make it extraordinary. The loaded skill has the focus styling patterns for this platform.

Multi-layered focus treatment:
- Scale transform
- Accent-colored border
- Glow effect (shadowColor = accentColor, high shadowOpacity, large shadowRadius)

Then add ONE advanced focus pattern (pick the best fit):

**Option A — Specular highlight**: Add a subtle top-edge light reflection on focused cards (1px semi-transparent white border-top + positioned gradient). Makes cards feel physically elevated. See the loaded skill's "Specular Highlight Focus" section.

**Option B — Focus-driven environment**: When a card receives focus, update a background tint or ambient color based on that item's primary color. Even a simple `backgroundColor` animation on the screen wrapper tied to the focused item's category color creates the Netflix "alive background" effect. See the loaded skill's "Focus-Driven Environment" section.

Then add a content-type flourish:
- Sports: diagonal accent stripe on focused cards
- Cooking: warm golden inner glow
- Music: neon bleed beyond the card boundary
- Fitness: sharp accent underline
- Kids: spring-like bounce

## STEP 6: Screen-specific atmosphere

Each screen should feel slightly different:
- **Home**: Most dramatic. Full atmospheric background, cinematic gradients.
- **Detail/Player**: More intimate, content-focused.
- **Settings/Search**: Clean and structured but still themed.

## STEP 7: Rail and section polish

- Section titles: accent-colored line beside/below, generous letter-spacing or uppercase for captions
- Generous vertical spacing between sections
- Cards: subtle surface color, minimal borders for definition against dark backgrounds

## STEP 8: Verify

Run: cd "{{appDir}}" && {{typeCheckCommand}} 2>&1 | head -20
Fix any errors.

Verify focus components are still present and overflow is still set correctly on focus containers. The loaded skill has the verification commands.

## Absolute constraints

- NEVER change component structure, navigation hierarchy, or screen routing
- NEVER remove the focus/navigation system components
- NEVER modify list item sizing values
- NEVER add packages to a shared package's devDependencies (use the loaded skill's dependency rules)
- NEVER use fonts smaller than the platform's minimum readable size
- NEVER reduce text contrast below 4.5:1
- Keep all overflow:visible declarations on focus containers
- NEVER add custom keyboard or focus event listeners
