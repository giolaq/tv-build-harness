# Vega Lifecycle Fixture

Use `../vega-lifecycle.json` when Vega SDK or VDA is unavailable. It replays successful SDK, device, build, install, launch, log, capture, and pull results through the same lifecycle runner as a live command.

The fixture is synthetic. It proves the workshop control flow and report contract, not that your app passed on a real device.
