# Resume Fixture

Use the Step 3 recording in `packages/mini-harness/steps/03-phases/fixtures/`. Run it once, open the checkpoint, then run the same command with `--resume`. You are done when completed phases are not repeated.
